[phases.setup]
nixPkgs = ["nodejs_20", "python311", "gcc", "gnumake"]

[phases.install]
cmds = ["npm ci --ignore-scripts", "npm rebuild better-sqlite3"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
